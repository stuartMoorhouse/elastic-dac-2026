name: Deploy to Prod
on:
  push:
    branches: [main]
jobs:
  validate-rules:
    name: Validate Detection Rules
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.12', cache: pip}
      - name: Install detection-rules CLI
        run: |
          pip install -e ".[dev]"
          pip install lib/kql lib/kibana
      - name: Validate TOML rules
        run: |
          PASS=0; FAIL=0
          for f in custom-rules/rules/*.toml; do
            [ -f "$f" ] || continue
            if python -m detection_rules validate-rule "$f"; then
              echo "PASS: $f"; PASS=$((PASS+1))
            else
              echo "FAIL: $f"; FAIL=$((FAIL+1))
            fi
          done
          echo "$PASS passed, $FAIL failed"
          [ "$FAIL" -eq 0 ]
  deploy:
    name: Upload rules to Prod
    runs-on: ubuntu-latest
    needs: validate-rules
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.12', cache: pip}
      - name: Install detection-rules CLI
        run: |
          pip install -e ".[dev]"
          pip install lib/kql lib/kibana
      - name: Upload rules to Prod Kibana
        run: |
          python -m detection_rules kibana \
            --kibana-url "${{ secrets.PROD_KIBANA_URL }}" \
            --api-key "${{ secrets.PROD_KIBANA_API_KEY }}" \
            import-rules \
            --overwrite \
            $(find custom-rules/rules -name "*.toml" | sort | tr '\n' ' ')
